pipeline {
    agent any

    stages {

        stage('Checkout') {
            steps {
                echo 'Repository downloaded'
            }
        }

        stage('Build') {
            steps {
                sh 'ls -la'
            }
        }

    }
}
